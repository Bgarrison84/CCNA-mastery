{
  "name": "ccna-mastery",
  "version": "1.0.0",
  "description": "Offline-first CCNA 200-301 gamified study app",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i css/input.css -o css/app.css --minify",
    "watch:css": "tailwindcss -i css/input.css -o css/app.css --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "vitest": "^1.6.0"
  }
}
